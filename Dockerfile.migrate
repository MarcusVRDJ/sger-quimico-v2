# Migration container that runs migrations and seeds the database
FROM node:20-alpine

RUN apk add --no-cache libc6-compat

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

# Run migrations and seed
CMD ["sh", "-c", "npm run db:migrate && npm run db:seed"]
